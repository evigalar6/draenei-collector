# Copy to `.env` for local runs (do not commit real AWS credentials).
#
# Default values below work out-of-the-box with the included MinIO service
# (local S3-compatible object store).

# S3 / MinIO
BUCKET_NAME=draenei-collector
S3_ENDPOINT_URL=http://minio:9000
S3_FORCE_PATH_STYLE=true

# Local MinIO credentials (also used by Airflow tasks via boto3)
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_DEFAULT_REGION=us-east-1

# Pipeline tuning
UPLOAD_BATCH_LIMIT=5

